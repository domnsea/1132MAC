CHURCH GUEST ZOOM — BUILD 2026-08-23-N

Throw away zip M and earlier. M was slow because it waited on leftover
parks, extra dialogs, and system_profiler (very slow on a 2015 Air).

HOW TO RUN (pick one)

A. Right-click  ChurchGuestZoom-OPEN-ME.command  → Open → Open
   Terminal should appear, then a dialog (it continues on its own after a few seconds).

B. Right-click  ChurchGuestZoom.app  → Open → Open
   A dialog should appear. Do not just double-click the first time.

If macOS says it is from an unidentified developer: Open anyway.

Quit 1132wtf-v94 first. Click Allow if Keychain asks.
If Keychain says no, guest Zoom still starts.

One Mac password box may appear. Look behind Terminal or other
windows. If you miss it, church Zoom still opens.

After Zoom opens: Settings → Audio → Microphone → VB-Cable
(or CABLE Output). If macOS asks to allow Microphone for Zoom, click OK.

Log: Desktop/ChurchGuestZoom-log.txt

Do not use BALLROOM_Mac_Universal_Reset_Kit.zip for church.
Do not open a raw .command from GitHub.
